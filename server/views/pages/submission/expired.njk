{% extends "../../partials/layout.njk" %}

{% set pageTitle = t('submission.expired.title') %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ pageTitle }}</h1>
      <p>{{ t('submission.expired.intro') }}</p>
    </div>
  </div>
{% endblock %}
