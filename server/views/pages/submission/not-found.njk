{% from "govuk/components/button/macro.njk" import govukButton %}
{% extends "../../partials/layout.njk" %}

{% set pageTitle = t('submission.notFound.title') %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ t('submission.notFound.heading') }}</h1>
      <p>{{ t('submission.notFound.checkAddressTyped') }}</p>
      <p>{{ t('submission.notFound.checkAddressPasted') }}</p>
      <p>{{ t('submission.notFound.linkOutOfDate') }}</p>
      <p>{{ t('submission.notFound.contactOfficer') }}</p>
    </div>
  </div>
{% endblock %}
