{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}

{% extends "../../../partials/layout.njk" %}

{% set pageTitle = t('submission.video.inform.title') %}

{% block beforeContent %}
  {{
    govukBackLink({
      text: t('common.buttons.back'),
      href: "/" + submissionId + "/questions/additional/" + additionalQuestionsCount if additionalQuestionsCount else "/" + submissionId + "/questions/assistance"
    })
  }}
{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ pageTitle }}</h1>
      <p>{{ t('submission.video.inform.intro') }}</p>
      <p>
        {{ t('submission.video.inform.comparison') }}
      </p>
      <p>{{ t('submission.video.inform.permissions') }}</p>
      <h2 class="govuk-heading-s">{{ t('submission.video.inform.rulesHeading') }}</h2>
      <p>{{ t('submission.video.inform.rulesIntro') }}</p>
      <ul class="govuk-list govuk-list--bullet">
        <li>{{ t('submission.video.inform.rules.litArea') }}</li>
        <li>{{ t('submission.video.inform.rules.plainBackground') }}</li>
        <li>{{ t('submission.video.inform.rules.faceInFrame') }}</li>
        <li>{{ t('submission.video.inform.rules.noHatSunglasses') }}</li>
      </ul>
      {{ govukInsetText({
        text: t('submission.video.inform.inset')
      }) }}
      {{
        govukButton({
        text: t('common.buttons.continue'),
        href: "/" + submissionId + "/video/record"
        })
      }}
    </div>
  </div>
{% endblock %}
