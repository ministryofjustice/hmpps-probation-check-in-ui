{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/inset-text/macro.njk" import govukInsetText %}

{% extends "../../../partials/layout.njk" %}

{% set pageTitle = t('submission.liveness.inform.title') %}

{% block beforeContent %}
  {{
    govukBackLink({
      text: t('common.buttons.back'),
      href: "/" + submissionId + ("/questions/additional/" + formData['additionalQuestions'].length if formData['additionalQuestions'] and formData['additionalQuestions'].length else "/questions/assistance")
    })
  }}
{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-l">{{ pageTitle }}</h1>
      <p>
        {{ t('submission.liveness.inform.intro') }}</p>
      <p>
        {{ t('submission.liveness.inform.comparison') }}
      </p>
      <p>{{ t('submission.liveness.inform.permissions') }}</p>
      <h2 class="govuk-heading-s">{{ t('submission.liveness.inform.rulesHeading') }}</h2>
      <p>{{ t('submission.liveness.inform.rulesIntro') }}</p>
      <ul class="govuk-list govuk-list--bullet">
        <li>{{ t('submission.liveness.inform.rules.faceInFrame') }}</li>
        <li>{{ t('submission.liveness.inform.rules.litArea') }}</li>
        <li>{{ t('submission.liveness.inform.rules.plainBackground') }}</li>
        <li>{{ t('submission.liveness.inform.rules.noHatSunglasses') }}</li>
      </ul>
      {{ govukInsetText({
        text: t('submission.liveness.inform.inset')
      }) }}
      {{
        govukButton({
        text: t('common.buttons.continue'),
        href: "/" + submissionId + "/liveness/record"
        })
      }}
    </div>
  </div>
{% endblock %}
