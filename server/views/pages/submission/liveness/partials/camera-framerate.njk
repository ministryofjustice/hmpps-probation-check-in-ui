{% from "govuk/components/button/macro.njk" import govukButton %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-l">{{ t('submission.liveness.partials.cameraFramerate.heading') }}</h1>
    <p>
      {{ t('submission.liveness.partials.cameraFramerate.intro') }}
    </p>
    <p>
      {{ t('submission.liveness.partials.cameraFramerate.advice') }}
    </p>
    <p class="govuk-!-font-weight-bold">{{ t('submission.liveness.partials.cameraFramerate.rulesHeading') }}</p>
    <p>{{ t('submission.liveness.partials.cameraFramerate.rulesIntro') }}</p>
    <ul class="govuk-list govuk-list--bullet">
      <li>{{ t('submission.liveness.partials.cameraFramerate.rules.faceInFrame') }}</li>
      <li>{{ t('submission.liveness.partials.cameraFramerate.rules.followInstructions') }}</li>
      <li>{{ t('submission.liveness.partials.cameraFramerate.rules.steadyPace') }}</li>
    </ul>
    <p>
      {{ t('submission.liveness.partials.cameraFramerate.fallbackHint') }}
    </p>
    <div class="govuk-button-group">
      {{ govukButton({ text: t('submission.liveness.partials.cameraFramerate.verifyAgain'), href: "/" + submissionId + "/liveness/record" }) }}
      {{ govukButton({ text: t('submission.liveness.partials.cameraFramerate.recordInstead'), classes: "govuk-button--secondary", href: "/" + submissionId + "/liveness/fallback-inform" }) }}
    </div>
  </div>
</div>
