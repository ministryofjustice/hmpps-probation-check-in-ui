{% from "govuk/components/button/macro.njk" import govukButton %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <h1 class="govuk-heading-l">{{ t('submission.liveness.partials.landscape.heading') }}</h1>
    <p>
      {{ t('submission.liveness.partials.landscape.intro') }}
    </p>
    <p>
      {{ t('submission.liveness.partials.landscape.advice') }}
    </p>
    <p class="govuk-!-font-weight-bold">{{ t('submission.liveness.partials.landscape.rulesHeading') }}</p>
    <p>{{ t('submission.liveness.partials.landscape.rulesIntro') }}</p>
    <ul class="govuk-list govuk-list--bullet">
      <li>{{ t('submission.liveness.partials.landscape.rules.faceInFrame') }}</li>
      <li>{{ t('submission.liveness.partials.landscape.rules.followInstructions') }}</li>
      <li>{{ t('submission.liveness.partials.landscape.rules.steadyPace') }}</li>
    </ul>
    <p>
      {{ t('submission.liveness.partials.landscape.fallbackHint') }}
    </p>
    <div class="govuk-button-group">
      {{ govukButton({ text: t('submission.liveness.partials.landscape.verifyAgain'), href: "/" + submissionId + "/liveness/record" }) }}
      {{ govukButton({ text: t('submission.liveness.partials.landscape.recordInstead'), classes: "govuk-button--secondary", href: "/" + submissionId + "/liveness/fallback-inform" }) }}
    </div>
  </div>
</div>
