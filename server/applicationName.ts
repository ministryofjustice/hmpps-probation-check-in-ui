/*
 * Kept free of imports so that it can be used by telemetry setup, which must run before any
 * other application module is loaded.
 */
const applicationName = 'hmpps-probation-check-in-ui'

export default applicationName
